Backend (Node.js + Express + MongoDB)

- Put your real MongoDB connection string into a `.env` file (don't commit secrets).
- Example `.env`:

  PORT=3000
  MONGO_URL=mongodb+srv://USERNAME:PASSWORD@cluster0.mongodb.net/referrals
  ADMIN_SECRET=your_admin_secret_here

- Install & run:
  npm install
  npm start
